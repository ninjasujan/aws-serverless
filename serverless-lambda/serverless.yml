service: serverless-lambda
frameworkVersion: "2"

provider:
  name: aws
  runtime: nodejs12.x
  lambdaHashingVersion: "20201221"
  region: us-east-1

functions:
  serverless:
    handler: handler.serverless
    events:
      - http:
          path: /
          method: get
